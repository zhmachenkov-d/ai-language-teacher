# Teacher service

Installable hexagonal package (`teacher_service`). HTTP listen/auth arrives in Story 1.2 — this package is installable and testable only in Story 1.1.

```bash
uv sync
uv run pytest
```
